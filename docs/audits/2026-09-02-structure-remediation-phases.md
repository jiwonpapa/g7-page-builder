# 코드 구조 개선 차수

기준: `0a6fcbae69ad0c0ad93aea145c8899c4b001ecfb`, 2026-09-02. [이전 코드 감사 개선](2026-09-02-code-audit-remediation.md)의 후속 작업이다.

현재 상태: **1차 전체 완료, 2차 코드·필수 검증·재감사 완료**. 1차 제품 검증 SHA는 `38ec067ed6dbb68c3ac09ce4c8572f1ec6524edc`, 문서 통합 기준은 `e9a3afb1d18b87e50c5abffc00fc80294764c96d`다. 2차 제품 검증 SHA는 `d59bbc07482e878c179ab784a0e301c4c7c5a1e3`이며 Adapter 2,634→178줄, 리치텍스트 901→99줄, 정적 부채 1,069→1,066이다. 신규 위반·우회 단언 규칙 위반은 0이다. [1차 마감 기록](2026-09-02-structure-phase-1.md)과 [2차 마감 기록](2026-09-02-structure-phase-2.md)에 실제 검사와 문서 통합·정식 종료 영수증을 구분한다. 3~6차는 아직 실행하지 않았다.

## 범위와 진행 방식

기존 블록 콘텐츠의 문구·이미지·샘플·프리셋 품질은 검사하지 않는다. PHP·TypeScript·JavaScript·CSS의 책임 분리, 검증 경계, 재사용, 테마와 하네스가 대상이다. 합성 문서로 코드 동작을 검증한다. 제품 기능 재기획, 운영 배포, 운영 데이터 준비·변환은 이번 코드 개선과 별도다.

차수는 아래 순서로 진행한다. 각 실행 단위는 정확 파일 24개 이내의 독립 worktree와 scoped task로 소유하고, 관련 검사 → Local 통합 → 통합 검증을 마친 뒤 다음 단위로 넘어간다. 한 차수가 여러 단위로 나뉘면 `1-A`, `1-B`처럼 기록한다. 앞 단위의 코드·검증이 완료되기 전에 의존하는 다음 구현을 시작하지 않는다. 사용자의 차수별 마감 지시에 따라 한 차수를 시작하면 그 안의 모든 단위·필수 보완·통합 검증·감사 기록·정식 소유권 종료까지 이어서 완료한다. 하위 단위 완료를 전체 차수 마감으로 대신하지 않는다.

파일 축소만으로 완료하지 않는다. 도메인 규칙·상태·렌더·외부 변환의 소유자가 명확해야 하고 기존 동작과 직렬화 형식이 유지되어야 한다. 단언을 다른 단언으로 바꾸거나, 색마다 이름만 붙이거나, 예외를 다른 파일로 옮겨 수치만 줄이지 않는다. 신규 예외·기존 상한 증가·전체 검사로의 자동 확대를 금지한다.

## 차수와 완료 기준

| 차수 | 구현 범위 | 완료 기준 | 관련 코드 증거 |
| --- | --- | --- | --- |
| 1. 타입·문서 경계 | `documents/types.ts`의 문서·블록·응답 계약 책임 분리, Puck 및 외부 입력 경계의 우회 단언 9건 해소 | 문서 도메인이 React/Puck/HTTP를 참조하지 않고, 검증·판별된 타입으로 값 생성. JSON 계약과 구형 읽기 유지. 새 파일 크기 제한 준수 | 타입 검사, 도메인/codec 왕복·잘못된 입력 거부, 선택 좌표·열 조작·외부 팩 경계 회귀 |
| 2. 편집기 조율 | `PuckEditorAdapter.tsx`의 설정/블록 렌더/명령 연결/상태/도구 UI 분리, `richTextEditing.tsx`의 편집 모델·선택·도구 분리 | 연결 컴포넌트에 검증·변환·통신 규칙 중복 없음. 새 거대 파일·순환 의존 없음. Puck 기본 히스토리/슬롯 소유권 유지 | 포커스·DOM 유지, 중첩 삽입/삭제·Undo, 저장 중 입력 보존, 실제 포인터/키보드 브라우저 |
| 3. 관리·카탈로그·공개 runtime | `PageBuilderManager.tsx`의 목록/리비전/메타데이터/스토어 화면과 사용 사례 분리, `catalogBlocks.tsx`의 codec/필드/렌더 분리, `pageEffects.ts`의 슬라이더·폼·효과 생명주기 분리 | 문서 변환이 React 렌더러를 경유하지 않음. 공개 runtime의 관리자 의존 없음. listener/timer/재초기화 소유권 명시 | 관련 관리 API/화면 회귀, 합성 블록 왕복, 공개 효과 중복 초기화/해제·반응형 동작 |
| 4. PHP 컴파일러 | `HtmlDocumentCompiler.php`의 문서 조율과 블록별 렌더러 분리 | escaping·URL·리치텍스트·진단·블록 순서·출력 해시 유지. renderer가 G7/DB/발행 상태를 소유하지 않음. 각 파일 제한 준수 | 합성 입력의 기존/신규 출력 동등성, 악성 입력 거부, compiler 및 발행 트랜잭션 회귀 |
| 5. 스타일·테마 | primitive/의미/컴포넌트 토큰 정리, 편집·관리·공개 UI 재사용, `!important`/반복 selector 해소, 편집기 CSS 책임 분리 | 배경·전경·테두리 함께 상속. 명시 사용자 설정 우선권 유지. portal/iframe에서 동일 의미. 중복값 이동으로 해소 처리하지 않음 | Stylelint·정적 부채 감소, 합성 화면의 computed style·대비·상속·선택 상태·등장 애니메이션 완료 위치 |
| 6. 재감사·종료 | 실제 감소량과 잔여 예외, import 방향·순환·타입/스타일 경계 및 하네스 선택 범위 재검토 | 동일 기준으로 전후 비교, 새 위반 0, 해소된 예외 제거, 관련 행동 검사와 소유권 종료 기록. 미해소 항목을 완료로 표시하지 않음 | 정적 결과·소스 지문, 차수별 검사 기록, 최종 integration-verify 및 NO_RELEASE 종료 |

## 시작 부채

정적 진단 1,079건은 실행 오류 개수가 아니다. 코드 수정 전 마지막 감사와 현재 소스가 일치하는 기준을 사용하며, 차수별 실제 감사 수치로 갱신한다.

| 규칙 | 시작 진단 |
| --- | ---: |
| 색 리터럴 | 908 |
| `!important` | 135 |
| selector 우선순위 | 16 |
| TypeScript 우회 단언 | 9 |
| 파일 크기 | 11 / 8개 파일 |

크기 대상은 `documents/types.ts`, `PuckEditorAdapter.tsx`, `catalogBlocks.tsx`, `richTextEditing.tsx`, `PageBuilderManager.tsx`, `pageEffects.ts`, `HtmlDocumentCompiler.php`, `page-builder-editor.css`다. TS의 줄 수와 AST 크기가 동시에 초과한 경우 진단이 둘이므로 파일 수와 구분한다.

## 순차 실행 단위

### 1차 — 타입·문서 경계

1. **1-A: 외부 입력 검증.** `blockAppearance.ts`와 `canvasContextState.ts`의 2개 우회 단언을 검증 후 명시 타입 구성으로 교체한다. 정상 구형 외형·요소 스타일·기본값 생략·입력 불변을 보존한다.
2. **1-B: Puck 기본 데이터.** layout hook의 제네릭과 선택 union, 열 조작 dispatch, 기본 slot props, 문서 envelope 보강, 설정 field 조합의 6개 우회 단언을 정리한다. 잘못된 타입의 값을 Puck 데이터로 포장하지 않는다.
3. **1-C: 외부 Block Pack 데이터.** 등록된 외부 컴포넌트와 기본 컴포넌트의 타입 경계를 명시해 codec의 남은 단언을 해소한다. 동적 외부 컴포넌트를 기본 블록 타입으로 거짓 선언하지 않고, 기본 블록 판별의 정밀도도 유지한다. 외부 원본 props와 Puck 인스턴스/편집 metadata는 별도 payload 경계로 분리하고 기본 블록 공통 보강·삭제 로직을 통과시키지 않는다. 공개 등록 이름과 canonical JSON은 유지한다. 외부 ComponentConfig의 defaultProps/fields/render/resolver 연결을 정의하고 충돌 키를 포함한 전체 props 왕복·신규 삽입·복제·Undo·필드 수정으로 확인한다. 단순 접두사 변경이나 삭제 목록 확대만으로 완료하지 않는다.
4. **1-D: 도메인·응답 계약.** 문서/블록/스타일 타입의 소유권을 나누고 API 응답 DTO를 API 쪽으로 이동한다. 소비 파일 수가 24개를 넘으면 책임 묶음별로 나누며 도메인→API 재수출을 임시 호환책으로 사용하지 않는다.

1-A에는 `blockAppearance.ts`가 기존 프리셋 비교를 선택하던 하네스 연결의 별도 선행 수정이 필요하다. 이 파일은 합성 테마/구조 및 실제 글자 편집 검사로 연결하고, 아직 수정하지 않은 카탈로그·반응형 렌더러의 검사 정책을 한꺼번에 변경하지 않는다.

1-D는 현재 소비 관계에 따라 다음 세 단위로 실행한다. 경로·타입 이름의 변경만으로 원본 JSON 계약이 바뀌지 않으며, 런타임 의미를 바꾸는 수정은 별도로 기록한다.

- **1-D1, 24파일:** API DTO 24개·184줄을 `api/resources.ts`로 옮기고 관련 소비자 21파일을 함께 변경한다. `types.ts`는 약 726줄로 내려가므로 크기 예외를 제거한다. 이미 상한인 Manager에는 별도 import를 덧붙이지 않고 API 클라이언트의 명시적 type export와 기존 API import를 이용한다. 정의 소유자는 `api/resources.ts`다.
- **1-D2, 7파일:** 기본 블록 식별자·Props/Item·구체 타입·기존 판별 함수를 `documents/builtinBlockContracts.ts`로 옮기고 codec·Puck 타입 및 API의 핵심 소비자를 새 소유자로 연결한다. 카탈로그 6파일은 기존 도메인 호환 진입점을 유지한다. 이를 위해 상한을 늘리거나 관련 없는 렌더 코드를 고치지 않는다.
- **1-D3, 14파일:** 공통 외형/동작/반응형을 `documents/blockPresentation.ts`, 구조 설정을 `documents/layoutContracts.ts`로 분리한다. 외형·동작·구조의 직접 소비자를 새 소유자로 연결하고, `types.ts`에는 범용 노드와 원본 Page/Site Part 문서의 실선언 및 도메인 호환 재수출을 남긴다. 타입을 조합하는 참조와 실행 시 순환 의존을 구분해 런타임 순환이 생기지 않았는지 확인한다. `CHANGELOG.md`에 1-D 전체의 계약 소유권 분리와 JSON 호환 보존을 기록한다.

타입 import/export만 바뀐 기존 소비 파일은 TypeScript AST로 나머지 구문·주석 위치·module/script 성격이 동일함을 증명한 경우에만 브라우저 선택에서 제외한다. 타입·관련 Unit·계층 검사는 계속 수행한다. 새 파일·도메인 파일·값 import·본문·JSX 변경·파싱/기준/도구 실패는 기존 매핑을 유지한다. 프리셋 검사 매핑 자체를 삭제하거나 파일명만으로 면제하지 않는다.

### 2·3차 — UI와 공개 runtime

2차는 아래 네 단위를 순서대로 완료한다. 앞 단위의 통합 검증 이후 다음 제품 구현을 시작한다. 새 소유 파일에 관련 검사를 연결하는 독립 하네스 작업은 제품 구현에 앞서 통합한다.

- **2-A:** Puck 설정·기본 블록 렌더·preview 입력 정규화·전체 사이트 캔버스·공유 편집 context를 분리한다.
- **2-B:** 라이브러리/헤더/선택 도구 UI와 블록·항목·동작 명령, 선택 도구의 배치 수명주기를 분리한다.
- **2-C:** 문서 세션, viewport, 캔버스 선택 이벤트, API 자원 조회의 소유권을 나누고 Adapter를 연결 컴포넌트로 정리한다.
- **2-D:** 리치텍스트 모델·선택·명령·떠있는 도구 배치·메뉴를 나누고 기존 필드 진입점을 유지한다.

2-C의 최종 브라우저 확인에서 옵션 hit-test가 한 번 실패했다. 원본 실패를 보존하고 같은 순간의 표시 상태를 기록하는 진단을 먼저 보완했다. 별도로 재현한 불필요한 floating 재안정화와 배치 소유자 추출을 2-C 차단 보완으로 처리한 뒤 최종 브라우저 8개를 통과했다. 2-D는 남은 모델·선택·명령·메뉴를 분리하고 메뉴 timer·편집기/readonly 입력 잔존을 보완했으며 최종 실제 글자 편집/접근 2개를 통과했다. 처음 실패 시점의 정확한 이벤트 원인이 입증됐다는 뜻으로 확대하지 않는다.

안정적인 React 컴포넌트/slot 함수 식별자와 Puck 기본 히스토리를 유지한다. 외부 원본 문서의 갱신만으로 편집 세션을 초기화하지 않으며, 문서 경계의 입력 거부·복구와 저장 중 입력 보존도 확인한다. 원 파일과 새 파일 모두 크기 기준을 적용한다. 기존 예외는 실제 측정에 맞춰 낮추고 기준 이내가 된 뒤 제거한다. 한 파일에서만 함수 이름을 찾는 검사도 실제 연결된 코드 소유자를 읽도록 보완하되 행동 보호 조건을 없애지 않는다.

3차는 관리자 목록·리비전·메타데이터·스토어, 카탈로그 공통 Frame, 카탈로그 codec/필드/렌더, 공개 데이터/필터, 공개 효과 초기화·해제 순으로 분리한다. 카탈로그 Frame은 같은 선택·외형·motion 연결의 중복을 줄이되 기본 블록의 문구·상품성 평가는 하지 않는다.

2-B 합성 명령 회귀에서 기존 `foundationCatalogBlocks.tsx`의 `appearance`가 명시 `spacing=normal`을 허용하지 않아 Buttons/Heading의 기본 `compact`로 바꾸는 왕복 결함이 드러났다. 2차 변경 전부터 같은 코드였으며 해당 카탈로그는 2차 수정 범위가 아니다. 3차 카탈로그 codec에서 명시값·기본값·잘못된 값의 보존 규칙과 함께 처리한다. 2-B의 Undo 검사는 왕복 가능한 `compact` fixture에서 원본 전체 비교를 유지하며 이 결함을 해소했다고 계산하지 않는다.

### 4차 — PHP 컴파일러

1. **4-A: 공통 검증·동작.** 속성 판별/오류 메시지의 `BlockPropertyReader`, 반응형/motion/visibility의 `BlockRuntimeCompiler`, 기존 타입 ID 소유자를 분리한다. 기존 compiler 한 파일의 87% coverage 하한을 본체+추출 객체의 같은 검사 범위로 보존하고 누락/미달 시 실패하게 한다. 파일 분리로 검증 하한을 낮추지 않는다.
2. **4-B: 기본 요소 렌더러.** Heading/RichText/Image/Buttons/ImageText/IconList 6종을 기존 renderer 계약으로 분리한다. 기대 출력 해시는 변경하지 않는다.
3. **4-C 이후: 나머지 렌더러.** 관련 책임별 6~10종씩, 각 단위 정확 24파일 이내로 나눈다. renderer가 본체를 역참조하는 wrapper/trait 추출은 사용하지 않는다.
4. **4-마감: 문서 조율.** 재귀 레이아웃과 진단 수집을 정리해 본체 크기 예외를 실제로 제거한다. 타입 ID·등록 우선권·검증 순서·escaping·compiler 버전과 출력 형식을 보존한다.

### 5차 — 스타일·테마

1. **5-A: 검사 계약.** `check-editor-layout-parity.mjs`의 `!important` 문자열 강제를 실제 계산 결과 검사로 전환한다. 기본 제목 700, 사용자 regular 400, 기존 Features 행간 기대값을 유지하고 충돌하는 host CSS를 합성 fixture에 넣는다.
2. **5-B: 편집 UI 토큰.** 기존 core 의미 토큰을 editor/manager/Site Part 컨트롤에서 재사용한다. 경고·오류·선택·그림자는 전경·배경·테두리를 함께 정의한다.
3. **5-C: 페이지 표면 토큰.** editor/public의 배경·글자·보조글자·테두리를 기존 theme 계약으로 통일한다. light/dark/system, 카드 중첩, 명시 색, iframe을 함께 검증한다.
4. **5-D: Site Shell.** Header/Footer/메뉴의 중복 기본값을 자체 테마 범위로 통일한다. 페이지 root 밖 출력·투명 헤더·불투명 메뉴·숨김·포커스 복귀·drawer 위치를 보존한다.
5. **5-E: 글자 우선순위.** 기본값→블록 외형→사용자 설정→Puck 입력 상속 순서를 명시하고 반복 selector와 대체 가능한 important를 제거한다. 접근성 숨김·reduced-motion 등 강제 선언은 일괄 삭제하지 않는다.
6. **5-F: 책임별 CSS 분리.** 토큰과 우선순위 정리가 끝난 UI chrome/라이브러리/캔버스/블록 외형/사용자 글자 규칙부터 분리한다. 새 경로에 정확한 합성 코드 검사를 연결하고 전체 카탈로그 검사로 확대하지 않는다. 각 CSS 파일은 1,000줄 이하로 유지한다.

남겨야 하는 기존 vendor·접근성 예외는 근거와 검증을 기록하며, 제거하지 않은 항목을 해소 건수에 포함하지 않는다. `@layer` 추가나 Stylelint 통과만으로 cascade 동등성을 주장하지 않는다.

## 운영·인프라 경계

인증된 G7 runtime이 없는 hosted CI는 실제 브라우저 통과로 인정하지 않는다. 차수별 제품 검증은 소유권을 확보한 Local runtime에서 수행한다. hosted CI용 runtime/인증과 최초 Site Part 발행 결과 이행은 별도 환경 결정·실행 증거가 필요하며, 로컬 코드 정비만으로 해결됐다고 보고하지 않는다.

## 실행 기록

| 단위 | 상태 | 범위·증거 |
| --- | --- | --- |
| 계획 | 작성 | 기준·순서·완료 조건 및 소유 범위별 실행 단위 정의 |
| 1-A | 완료 | 우회 단언 9→7. 입력 준비 검사 보완 후 실제 브라우저 8개 통과. 검증 SHA `a2836a4` |
| 1-B | 완료 | 우회 단언 7→1, 정적 부채 1,077→1,071. 실제 브라우저 8개 통과. 검증 SHA `b4fb157` |
| 1-C | 완료 | 외부 payload·편집 metadata 분리, 우회 단언 1→0, 실제 브라우저 7개 통과. 검증 SHA `4ad854a` |
| 1-D1 | 완료 | API DTO 24개 소유권 분리, types.ts 크기 예외 제거. 실제 발행 흐름 1개 통과. 검증 SHA `ca5004a` |
| 1-D2 | 완료 | 기본 블록 선언 124개 분리·기존 계약 보존. 실제 브라우저 5개 통과. 검증 SHA `1d674e5` |
| 1-D3 | 완료 | 공통 9·레이아웃 6 선언 분리. 실제 브라우저 6개 통과. 검증 SHA `38ec067` |
| 1차 재감사·마감 | 완료 | 232파일/신규 위반 0/정적 부채 1,069. 정적 런타임 순환 0. 문서 통합·NO_RELEASE 종료 영수증 별도 보존 |
| 2-A~D | 완료 | 설정/렌더·도구/명령·세션/API·리치텍스트 소유권 분리 및 발견 결함 보완. 최종 제품 검증 SHA `d59bbc0` |
| 2차 재감사·마감 | 마감 기록 참조 | 255파일/신규 위반 0/부채 1,066, 순환·문서 역참조 0. 문서 통합·NO_RELEASE 종료 영수증 별도 보존 |
| 3~6차 | 예정 | 위 순서와 완료 조건에 따라 실행 |

[1차 구현·실패 보완·검증 기록](2026-09-02-structure-phase-1.md)에 제출/통합 SHA, 실제 검사, 부채 전후 수치를 기록한다. 다음 차수의 계획을 작성했다는 이유로 해당 차수를 완료 처리하지 않는다.
