# G7 네이티브 페이지 편집 확장 개발 계획

<!-- editor-plan:native-editor-20260907 -->

상태: **NE1~NE6 완료(6/6)**. PB 0.35.0과 검증한 G7 공개 호스트를 g7devops.com에 배포하고 실제 운영 일반 페이지의 네이티브 편집·Undo·재열기 및 공개 출력을 확인했다. 상세 증거와 지원 한계는 [NE6 기록](../audits/2026-09-08-native-editor-ne6.md) 및 [진척 원장](editor-progress.json)을 따른다. 계획 차수 완료는 모든 테마/구조 지원이나 전체 제품 상용 완성 판정이 아니다.

[편집 정책](editing-policy.md) · [개발 헌법](../development-constitution.md) · [진척 원장](editor-progress.json) · [표시판](editor-progress.md) · [코드 근거](../audits/2026-09-07-native-editor-governance.md)

## 실행 규율

1. 한 차수의 의존성·수용 기준을 충족한 뒤 다음 차수를 진행한다. 차수당 구현 task는 정확한 24개 이하 파일 scoped lease로 나눌 수 있으며 이 표의 후보 경로는 소유권이나 파일 생성 지시가 아니다.
2. G7 측 공개 계약이 필요한 작업은 이 저장소 내부 import나 임시 SDK로 우회하지 않는다. 실제 공개 API·버전·서명·소유자·실패 의미를 확인하고 부족하면 원인과 선행 작업을 기록한다.
3. 각 차수에서 실제 편집·Undo·저장·재열기를 검증한다. 성공 입력 재사용과 실패 downstream 재실행을 유지하며 관련 없는 전체 검사·콘텐츠 증설로 범위를 넓히지 않는다.
4. 정책/소스/통합/실동작/배포 증거를 구분한다. 문서 checker는 완료 근거 형식·연결을 확인하며 제품 성공이나 현재 HEAD를 자동 보증하지 않는다.
5. 기존 [EP 계획](editor-plan-puck-20260907.md)과 [4/11 완료 원장](editor-progress-puck-20260907.json)은 해시로 보존한다. 기존 미완료 요구를 완료나 영구 제외로 바꾸지 않는다.

## NE1 — 공식 연동 계약과 원본 보존

<!-- editor-item:NE1 -->
<!-- editor-acceptance:NAT-01 -->

선행: H01~H04 공식 계약 확보; G7 수정은 별도 승인 필요. 수용 기준 `NAT-01`.

- H01~H03의 문맥·경로·명령 계약을 실제 G7 공개 API와 고정 기준 SHA로 확보한다. H04 패널·선택·내보내기 계약도 서명·소유자·제공 방식을 확정한다.
- 호스트가 제공하지 않는 API를 모사하지 않는다. 필요한 G7 변경은 별도 승인·lease·해당 저장소 검사로 수행한다. 부족하면 완료 처리하지 않는다.
- 별도 native entry, G7 React 공유, 등록 이름 충돌·수명, 지원 템플릿/spec 연결과 최소 무손실 node adapter를 검증한다.

**마감:** 지원 템플릿의 실제 일반 페이지에서 한 노드 편집→Undo/Redo→저장→재열기. 수정 외 의미 diff 0, 출처·표현식·번역·반복·actions·responsive·알 수 없는 필드 보존. readonly·라우트 전환·늦은 응답은 무변경.

필수 증거: `host-contract`, `unit`, `browser`. 전용 실행 요약에 기준 SHA·환경·실행 명령·대상·결과·남은 한계를 기록한다.

## NE2 — 내용·스타일·미디어 편집

<!-- editor-item:NE2 -->
<!-- editor-acceptance:NAT-02 -->

선행: NE1. 수용 기준 `NAT-02`.

- 현재 spec의 필드·상속·기본값·reset과 허용 스타일을 연결한다. 짧은 소수 옵션은 공통 라디오/분할 버튼, 긴·다수 옵션은 Select를 쓴다.
- G7 첨부 API로 업로드·목록·선택·교체를 연결하고 템플릿 전체 목록과 페이지 범위를 구분한다. 자산 참조 해제를 파일 삭제로 처리하지 않는다.
- 템플릿 토큰·원본 class/style·표현식을 보존하고 편집 확장 CSS를 scope 안에서 재사용한다.

**마감:** 지원된 문구·링크·이미지·alt·비율·스타일을 같은 노드 명령으로 편집하고 Undo·저장·재열기 결과를 비교. 바인딩 보존, 업로드 취소·실패·늦은 응답·문서 전환에서 오적용 0.

필수 증거: `unit`, `browser`. 전용 실행 요약에 기준 SHA·환경·실행 명령·대상·결과·남은 한계를 기록한다.

## NE3 — 내부 트리·반복·동적 요소 편집

<!-- editor-item:NE3 -->
<!-- editor-acceptance:NAT-03 -->

선행: NE2. 수용 기준 `NAT-03`.

- G7 nesting·출처·history 공식 경로와 컴포넌트별 허용 위치를 연결한다. 모든 구조를 단일 index로 취급하지 않는다.
- H05~H07의 지원 renderer/spec·병합·반복 이미지 편집 계약을 충족한다. 기존 반복 폼을 단순 widget 등록으로 확장했다고 가정하지 않는다.
- 동적 Slider는 대표 기술 fixture로 검증한다. 편집 중 자동재생/선택과 공개 런타임 수명·키보드·움직임 줄이기를 구분한다. 디자인 콘텐츠 증설은 제외한다.

**마감:** children·prop 내부 트리·배열·반복 템플릿의 삽입/이동/복제/삭제 및 거부 경로를 확인. ID·참조·출처 보존, 한 명령 Undo/Redo, Slider 항목 편집과 공개 동작 검증.

필수 증거: `unit`, `browser`, `runtime`. 전용 실행 요약에 기준 SHA·환경·실행 명령·대상·결과·남은 한계를 기록한다.

## NE4 — 사용자 조합 저장과 재삽입

<!-- editor-item:NE4 -->
<!-- editor-acceptance:NAT-04 -->

선행: NE3. 수용 기준 `NAT-04`.

- H04의 공식 선택·내보내기 계약을 사용한다. 선택 노드 스냅샷·페이지 본문 조합의 지원 범위를 명시하고 PB 패턴과 포맷을 구분한다.
- 재사용 스냅샷은 PB가 소유할 수 있지만 원본 G7 페이지의 두 번째 저장본으로 동기화하지 않는다. 내보내기·가져오기는 의존성·권한·충돌을 검사한다.
- 라이브러리 UX의 이름·빈 상태·삭제 의미를 통일한다. 판매 킷·연결형 전역 컴포넌트를 자동으로 추가하지 않는다.

**마감:** 선택 조합 저장→목록→다른 허용 위치 삽입→Undo→저장→재열기. 포맷·버전·자산·컴포넌트 호환 거부와 새 ID/내부 참조 재연결을 검증.

필수 증거: `unit`, `browser`. 전용 실행 요약에 기준 SHA·환경·실행 명령·대상·결과·남은 한계를 기록한다.

## NE5 — 삽입·설정·미리보기·저장 흐름 연결

<!-- editor-item:NE5 -->
<!-- editor-acceptance:NAT-05 -->

선행: NE4. 수용 기준 `NAT-05`.

- 공식 전역 패널/선택 API에 삽입과 사용자 조합을 연결한다. G7의 기존 트리·페이지 설정·반응형·저장 UI를 복제하지 않는다.
- 내용 폭과 전체 배경 폭은 지원 템플릿을 따른다. 정보 배너·중복 제목·Select 남용은 확장이 소유한 UI에서 고치며 호스트 DOM/CSS를 무단 덮지 않는다.
- 미지원 capability와 실패 사유를 해당 기능에 표시한다. 임의 테마 라우트/메뉴 CRUD나 native 초안/예약 발행을 끼워 넣지 않는다.

**마감:** 찾기→위치 선택→삽입→상세 편집→기존 페이지 설정→미리보기→저장→재열기 흐름을 실제 일반 페이지에서 확인. 공개 영향·문서 제목·라우트·오류·취소가 구분됨.

필수 증거: `unit`, `browser`. 전용 실행 요약에 기준 SHA·환경·실행 명령·대상·결과·남은 한계를 기록한다.

## NE6 — 호환·회귀·릴리스

<!-- editor-item:NE6 -->
<!-- editor-acceptance:NAT-06 -->

선행: NE5. 수용 기준 `NAT-06`.

- 지원 G7/테마/spec/renderer/CSS 조합과 editor 비활성·번들 실패 때 무손상 동작을 검증한다. 동적 renderer의 공개 출력과 fallback도 지원 범위에 넣는다.
- G7 content API의 빈 객체/배열 종류 보존을 별도로 검증하고 확인된 정규화 문제를 해결한다. PB 조합의 원문 보관 시험으로 G7 저장의 무손실을 대신하지 않는다.
- H08 실제 병렬 저장을 시험하고 재현 문제는 G7 공개 저장 경계에서 해결한다. 원본 손실·다른 노드 오적용·권한 우회·필수 계약 부재가 남으면 마감하지 않는다.
- 변경 관련 PC/태블릿/모바일·키보드/포커스·미디어/저장 실패 회귀와 승인된 커밋·푸시·배포·복구 증거를 구분한다. 제품 검증을 이 차수까지 전부 미루지 않는다.

**마감:** 검증한 G7/지원 템플릿 조합과 기존 PB 문서·발행본·주소가 공존. 실제 동시 저장·권한·장애 복구·반응형 공개 출력 통과 후 승인 범위의 릴리스와 운영 재열기/공개 결과 증거 기록.

필수 증거: `compatibility`, `browser`, `concurrency`, `deployment`. 전용 실행 요약에 기준 SHA·환경·실행 명령·대상·결과·남은 한계를 기록한다.

## 독립 PB 개선 1차 — 우측 설정창 (UI1)

<!-- editor-item:UI1 -->
<!-- editor-acceptance:UI-01 -->

2026-09-08 사용자 승인 범위. NE1~NE6의 완료 이력과 별도인 독립 PB 개선이며, 후속 차수의 구현 승인을 포함하지 않는다.

- PAGE 주요 6개 선택 컨트롤: 연결된 선택 버튼, 색상 견본, 보충 도움말, 반복 박스·설명 축소.
- 공통 배치 4개: 기능명 복원, 짧은 정렬 옵션은 아이콘 선택, 선택지가 많은 폭·높이는 Select 유지.
- 반복 배경/여백 및 짧은 제목 단계는 같은 선택 컴포넌트 사용. 저장값·Puck 명령·문서 계약 보존.
- 블록 기기별 공통 스타일은 접기와 별도 지정 개수 요약. PC 편집 정책 유지.
- 편집기 JS/CSS 내용 해시 URL로 기존 캐시와 구분. G7 코어나 전체 배포 인프라 변경 없음.

마감: 대표 블록에서 선택·키보드·Undo/Redo·저장·재열기, 좁은 패널의 잘림, 캐시된 세션의 버전 URL을 확인한다. 관련 검사만 실행하고 성공 입력은 재사용한다.

제외: Hero 모바일 출력 불일치, 슬라이더 HTML 라벨, 내부 자식 구조 확장, 콘텐츠/킷 증설, 발행·운영 배포.

완료 증거(로컬 통합·동작 검증, 운영 미배포): [1차 기록](../audits/2026-09-08-inspector-ux1.md).

## 독립 PB 개선 2차 — 편집 출력 일치 (UI2)

<!-- editor-item:UI2 -->
<!-- editor-acceptance:UI-02 -->

선행 UI1. 승인 범위는 기존 Hero의 반응형 정렬·글자 비율과 슬라이더 항목 요약 표시다.

- 공통·태블릿·모바일 값을 캔버스/컴파일 미리보기/발행에서 비교한다. 공통값과 별도 기기값, 기존 원본·저장 계약을 보존한다.
- 슬라이더 요약은 읽을 수 있는 텍스트로 표시하며 원래 제목 서식은 변경하지 않는다.
- 마감은 관련 단위·실제 브라우저·저장/재열기·코드 검사와 차수 커밋·로컬 통합이다.
- 새 블록·킷·내부 구조 확장·이력 엔진·G7 코어·운영 배포는 제외한다.

증거: [2차 기록](../audits/2026-09-08-editor-ux2.md).
