# 독립 PB 개선 6차 — Hero·ImageText 버튼 구역

상태: 6차 구현·로컬 통합 완료. 관련 브라우저 19건 통과. 운영 미배포.

## 고정 범위와 소유권

사용자가 구조 편집 상태에서 `버튼 구역으로 편집`을 누를 때만 기존 primaryCta/primaryLink 문구·URL·지원된 문구 스타일을 Buttons 항목으로 옮긴다. 부모의 기존 버튼 입력은 숨기고 단일 Puck replace가 전환 및 Undo를 소유한다.

원본은 `g7-page-builder/v2`의 명시적 `slots.actions`다. 빈 배열도 소유권을 유지한다. 편집기 내부 actionsEnabled는 이 슬롯 존재에서 복원하며 별도 영속 필드가 아니다. 기존 block_version=1 속성 형식은 바꾸지 않고 선택형 슬롯을 추가한다. 이전 문서를 여는 것만으로 스키마나 버튼을 변환하지 않는다.

기존 props와 actions를 동시에 소유한 입력은 TS/PHP 구조 정책과 컴파일러가 거부한다. 기존 extra·본문·이미지·ID는 보존한다. 버튼 구역은 Buttons 0~1개, 항목은 기존 1~3개를 사용한다. 전체 깊이와 개수 제한을 유지한다. 삭제 이후 저장·재열기에서 기존 버튼을 복구하지 않는다.

## 완료 기준

관련 단위 검사, 실제 브라우저의 기존 문서/전환/편집/삭제/빠른 Undo/저장·재열기/미리보기·발행/읽기 전용을 확인한다. 새 콘텐츠·킷·테마·운영 배포는 범위 밖이다.

## 검증 경과

TS 버튼 전환/원본 왕복/빈 구역/이중 소유 거부 12건, PHP 기존·새 슬롯 컴파일 및 거부 4건/49 assertions 통과. 기존 Hero·ImageText extra 브라우저 흐름 2건 통과.

새 브라우저 검사의 초기 비교는 서버 저장 전 빈 문자열 fixture를 저장 후 정규화된 원본과 비교해 실패했다. 저장 직후 API에서 읽은 기준 원본과 Undo 결과를 비교하도록 수정했다. 이 실패를 기능 완료 증거로 사용하지 않는다. 화면 확인에서 전환 버튼의 기본 스타일을 발견해 기존 공통 컨트롤 스타일로 맞췄다.

두 번째 실행에서는 전환·즉시 Undo·재전환·단일 원본 저장이 통과했다. 이어지는 테스트가 Puck의 숨겨진 패널과 표시된 패널을 함께 선택해 strict locator 오류로 멈췄다. 배열/입력/추가 버튼 선택자를 표시된 패널로 한정했다. 실제 추가·편집 동작을 실행하지 않은 상태를 완료로 기록하지 않는다.

## 실제 버튼 편집 확인

`heroComposition.editorInteractionQuality.spec.ts` desktop 4건 통과(52.9초). 기존 extra 2건과 새 actions 2건을 끝까지 실행했다. 기존 CTA 전환과 즉시 Undo, 문구·URL 편집, 버튼 항목 3개 및 추가 제한, 삭제 직후 Undo, 저장·재열기, 미리보기·발행 링크, 빈 구역의 재열기와 재삽입, 이중 소유 API 거부, 태블릿 읽기 전용을 확인했다. 편집 캔버스의 링크 클릭으로 실제 주소로 이동하지 않는다.

- 제품 제출 SHA: `0babba86e015be593b0e35259e26d3c3b8c9b7af`.
- 실제 브라우저 증거: `output/playwright/gates/integration-editor-ux6-20260909/7705cef1abb3a7afb734953b9800abf9e8cda27e9489986731a302cf1705efcd/62f21a72f1f24a63ad068196a25b1182/`.
- 같은 경로의 Hero/ImageText actions 화면을 직접 확인했다. 실제 중첩 Outline과 3개 버튼/필드 조작 화면이며 완성 디자인 상품의 품질 승인은 아니다.
- 변경된 컴파일러의 관련 Xdebug gate: facade 93.68%, compiler-family 89.36%, 하한 87% 통과.

사용 경로: 구조 편집 사용 → Hero 또는 이미지 + 텍스트 선택 → 내부 구성 → 버튼 구역으로 편집 → 버튼 편집. 기존 버튼 값이 있는 문서는 전환할 때 한 항목으로 옮기며, 빈 구역에서는 버튼 추가로 다시 넣는다.

## 차수 마감

제품 로컬 통합 SHA: `2d24d9fc14ec0f13426a0f385d69ed2d5cd5f993`.

최종 관련 브라우저는 Hero·ImageText 4건, Card 2건, 카탈로그 코드 3건, 텍스트/도구 조작 2건, 구조·테마 6건, 문서/중첩 저장·발행·복원 2건으로 총 19건 통과했다. TypeScript strict, 관련 TS/PHP 단위 검사, 구조 검사, Pint·PHPStan과 사전 빌드된 실제 브라우저 자산으로 확인했다. 실패한 중간 실행을 성공 건수에 합산하지 않았다.

기존 성공 입력의 gate를 재사용하고 변경/실패 관련 검사를 수행했다. 전체 콘텐츠 감사·새 블록 콘텐츠·페이지킷·테마 제작으로 범위를 확대하지 않았다. G7 코어/네이티브 저장·편집 API는 변경하지 않았다. 버전 발행·push·운영 배포는 실행하지 않았다. 현재 등록한 개선 범위의 마지막 차수는 UI6이며 전체 제품 상용 완성을 의미하지 않는다. 후속 차수는 범위 확정이 필요하다.
